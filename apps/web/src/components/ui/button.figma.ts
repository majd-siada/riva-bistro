/**
 * Figma Code Connect stub — RIVA BISTRO Button
 *
 * After Figma MCP authentication, create the Button component in Figma and
 * replace <FIGMA_BUTTON_NODE> with the node URL.
 *
 * @example
 * figma.connect(Button, "https://figma.com/design/...", {
 *   props: {
 *     variant: figma.enum("Variant", {
 *       Primary: "default",
 *       Secondary: "secondary",
 *       Gold: "gold",
 *       Outline: "outline",
 *       Ghost: "ghost",
 *     }),
 *     size: figma.enum("Size", { Small: "sm", Default: "default", Large: "lg" }),
 *     children: figma.string("Label"),
 *   },
 *   example: ({ variant, size, children }) => (
 *     <Button variant={variant} size={size}>{children}</Button>
 *   ),
 * });
 */

export {};
