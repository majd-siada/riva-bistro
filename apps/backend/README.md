# Django backend for Riva Bistro

See the root [README](../../README.md) for full setup instructions.

## Local notes

- Settings module: `config.settings`
- Health: `GET /api/v1/health/`
- OpenAPI: `GET /api/v1/schema/`
